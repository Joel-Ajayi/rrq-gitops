apiVersion: monitoring.coreos.com/v1
kind: Prometheus
metadata:
  name: kube-prometheus-stack-prometheus
  namespace: observability
spec:
  storage:
    volumeClaimTemplate:
      spec:
        resources:
          requests:
            storage: 50Gi
