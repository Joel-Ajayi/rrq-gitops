apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-worker
  namespace: rrq
  labels:
    app.kubernetes.io/name: fraud-worker
    app.kubernetes.io/part-of: rrq
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app.kubernetes.io/name: fraud-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: fraud-worker
        app.kubernetes.io/part-of: rrq
    spec:
      terminationGracePeriodSeconds: 30
      initContainers:
        - name: wait-for-infra
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              until nc -z merchants-db-rw.rrq 5432 2>/dev/null; do
                echo "Waiting for merchants-db Postgres..."
                sleep 2
              done
              until nc -z shard-a-rw.rrq 5432 2>/dev/null; do
                echo "Waiting for shard-a Postgres..."
                sleep 2
              done
              until nc -z shard-b-rw.rrq 5432 2>/dev/null; do
                echo "Waiting for shard-b Postgres..."
                sleep 2
              done
              until nc -z rrq-kafka-bootstrap.rrq 9092 2>/dev/null; do
                echo "Waiting for Kafka..."
                sleep 2
              done
              until nc -z redis.redis 6379 2>/dev/null; do
                echo "Waiting for Redis..."
                sleep 2
              done
      containers:
        - name: fraud-worker
          image: rrq/fraud-worker
          imagePullPolicy: Never
          lifecycle:
            preStop:
              exec:
                command:
                  - sleep
                  - "5"
          envFrom:
            - configMapRef:
                name: platform-config
            - configMapRef:
                name: fraud-worker-config
          env:
            - name: MERCHANTS_DB_URI
              valueFrom:
                secretKeyRef:
                  name: merchants-db-app
                  key: uri
            - name: SHARD_A_URI
              valueFrom:
                secretKeyRef:
                  name: shard-a-app
                  key: uri
            - name: SHARD_B_URI
              valueFrom:
                secretKeyRef:
                  name: shard-b-app
                  key: uri
            - name: REDIS_HOST
              value: "fraud-redis.rrq.svc.cluster.local"
            - name: REDIS_PORT
              value: "26379" #sentinel port
            - name: REDIS_MASTER_NAME
              value: "mymaster" #default
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: redis-password
            - name: KAFKA_BROKERS
              value: "kafka-cluster-kafka-bootstrap.rrq.svc.cluster.local:9092"
          livenessProbe:
            exec:
              command:
                - pgrep
                - service
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            exec:
              command:
                - pgrep
                - service
            initialDelaySeconds: 10
            periodSeconds: 10
          resources:
            requests:
              cpu: 21m
              memory: 65Mi
            limits:
              cpu: 21m
              memory: 65Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app.kubernetes.io/name
                      operator: In
                      values:
                        - fraud-worker
                topologyKey: kubernetes.io/hostname
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: fraud-worker
  namespace: rrq
spec:
  scaleTargetRef:
    name: fraud-worker
  minReplicaCount: 3
  maxReplicaCount: 3
  triggers:
    - type: kafka
      metadata:
        bootstrapServers: rrq-kafka-bootstrap.rrq.svc.cluster.local:9092
        consumerGroup: fraud-worker
        topic: jobs
        lagThreshold: "10500"
        offsetResetPolicy: earliest
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: fraud-worker
  namespace: rrq
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: fraud-worker
