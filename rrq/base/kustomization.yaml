# Shared base; overlays patch per-env. Stateful topology identical in both.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - config/configmap.yaml
  - postgres
  - kafka
  - services
  - gateway # Envoy Gateway + Gateway API edge routing
