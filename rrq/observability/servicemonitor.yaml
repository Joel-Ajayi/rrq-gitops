apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: otel-collector
  namespace: observability
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: gateway-collector #tell prometheus to monitor this service pod and scrape metrics
  endpoints:
    - port: prometheus
      interval: 15s
      path: /metrics
