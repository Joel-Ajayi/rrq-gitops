apiVersion: v1
kind: ConfigMap
metadata:
  name: rrq-svc-webhook-worker
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  rrq-svc-webhook-worker.json: |-
    {
      "title": "RRQ Webhook Worker",
      "uid": "rrq-svc-webhook-worker",
      "version": 1,
      "refresh": "5s",
      "time": { "from": "now-1h", "to": "now" },
      "timepicker": {},
      "tags": ["rrq", "tier-2"],
      "panels": [
        {
          "type": "timeseries",
          "title": "Latency P50/P95/P99",
          "gridPos": { "h": 8, "w": 3, "x": 0, "y": 0 },
          "targets": [
            { "expr": "histogram_quantile(0.50, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"webhook-worker\"}[5m])) by (le))", "legendFormat": "P50" },
            { "expr": "histogram_quantile(0.95, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"webhook-worker\"}[5m])) by (le))", "legendFormat": "P95" },
            { "expr": "histogram_quantile(0.99, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"webhook-worker\"}[5m])) by (le))", "legendFormat": "P99" }
          ],
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "fieldConfig": { "defaults": { "unit": "s" } }
        },
        {
          "type": "timeseries",
          "title": "Request Rate (RPS)",
          "gridPos": { "h": 8, "w": 3, "x": 3, "y": 0 },
          "targets": [{
            "expr": "sum(rate(rrq_consumer_commits_total{service_name=\"webhook-worker\"}[1m])) by (topic)",
            "legendFormat": "{{topic}}"
          }],
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "fieldConfig": { "defaults": { "unit": "cps" } }
        },
        {
          "type": "timeseries",
          "title": "Error Rate by Code",
          "gridPos": { "h": 8, "w": 3, "x": 6, "y": 0 },
          "targets": [{
            "expr": "sum(rate(rrq_infrastructure_errors_total{service_name=\"webhook-worker\"}[5m])) by (component)",
            "legendFormat": "{{component}}"
          }],
          "datasource": { "type": "prometheus", "uid": "prometheus" }
        },
        {
          "type": "timeseries",
          "title": "CB / Lag / Processing P99",
          "gridPos": { "h": 8, "w": 3, "x": 9, "y": 0 },
          "targets": [
            { "expr": "rrq_circuit_breaker_state{service_name=\"webhook-worker\",circuit_breaker=~\"merchant-.*\"}", "legendFormat": "{{circuit_breaker}}" },
            { "expr": "rrq_consumer_lag_messages{service_name=\"webhook-worker\"}", "legendFormat": "Kafka lag" },
            { "expr": "histogram_quantile(0.99, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"webhook-worker\"}[5m])) by (le))", "legendFormat": "Processing P99" }
          ],
          "datasource": { "type": "prometheus", "uid": "prometheus" }
        },
        {
          "type": "timeseries",
          "title": "Outbound HTTP Latency (P50/P95/P99)",
          "gridPos": { "h": 8, "w": 3, "x": 12, "y": 0 },
          "targets": [
            { "expr": "histogram_quantile(0.50, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"webhook-worker\",span_kind=\"SPAN_KIND_CLIENT\"}[5m])) by (le))", "legendFormat": "P50" },
            { "expr": "histogram_quantile(0.95, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"webhook-worker\",span_kind=\"SPAN_KIND_CLIENT\"}[5m])) by (le))", "legendFormat": "P95" },
            { "expr": "histogram_quantile(0.99, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"webhook-worker\",span_kind=\"SPAN_KIND_CLIENT\"}[5m])) by (le))", "legendFormat": "P99" }
          ],
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "fieldConfig": { "defaults": { "unit": "ms" } }
        }
      ]
    }
