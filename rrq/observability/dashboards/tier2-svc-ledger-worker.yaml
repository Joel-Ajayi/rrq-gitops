apiVersion: v1
kind: ConfigMap
metadata:
  name: rrq-svc-ledger-worker
  namespace: observability
  labels:
    grafana_dashboard: "1"
data:
  rrq-svc-ledger-worker.json: |-
    {
      "title": "RRQ Ledger Worker",
      "uid": "rrq-svc-ledger-worker",
      "version": 1,
      "refresh": "5s",
      "time": { "from": "now-1h", "to": "now" },
      "timepicker": {},
      "tags": ["rrq", "tier-2"],
      "panels": [
        {
          "type": "timeseries",
          "title": "Latency P50/P95/P99",
          "gridPos": { "h": 8, "w": 3, "x": 0, "y": 0 },
          "targets": [
            { "expr": "histogram_quantile(0.50, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"ledger-worker\"}[5m])) by (le))", "legendFormat": "P50" },
            { "expr": "histogram_quantile(0.95, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"ledger-worker\"}[5m])) by (le))", "legendFormat": "P95" },
            { "expr": "histogram_quantile(0.99, sum(rate(traces_span_metrics_duration_seconds_bucket{service_name=\"ledger-worker\"}[5m])) by (le))", "legendFormat": "P99" }
          ],
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "fieldConfig": { "defaults": { "unit": "s" } }
        },
        {
          "type": "timeseries",
          "title": "Request Rate (RPS)",
          "gridPos": { "h": 8, "w": 3, "x": 3, "y": 0 },
          "targets": [{
            "expr": "sum(rate(rrq_consumer_commits_total{service_name=\"ledger-worker\"}[1m])) by (topic)",
            "legendFormat": "{{topic}}"
          }],
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "fieldConfig": { "defaults": { "unit": "cps" } }
        },
        {
          "type": "timeseries",
          "title": "Error Rate by Code",
          "gridPos": { "h": 8, "w": 3, "x": 6, "y": 0 },
          "targets": [{
            "expr": "sum(rate(rrq_infrastructure_errors_total{service_name=\"ledger-worker\"}[5m])) by (component)",
            "legendFormat": "{{component}}"
          }],
          "datasource": { "type": "prometheus", "uid": "prometheus" }
        },
        {
          "type": "timeseries",
          "title": "CB / Saga / Backoff",
          "gridPos": { "h": 8, "w": 3, "x": 9, "y": 0 },
          "targets": [
            { "expr": "rrq_circuit_breaker_state{service_name=\"ledger-worker\",circuit_breaker=~\"shard-.*\"}", "legendFormat": "{{circuit_breaker}}" },
            { "expr": "rrq_consumer_lag_messages{service_name=\"ledger-worker\"}", "legendFormat": "Kafka lag" },
            { "expr": "rrq_saga_unresolved_count", "legendFormat": "Saga unresolved" },
            { "expr": "sum(rate(rrq_consumer_backoff_duration_seconds_sum{service_name=\"ledger-worker\"}[5m]))", "legendFormat": "Backoff" }
          ],
          "datasource": { "type": "prometheus", "uid": "prometheus" }
        }
      ]
    }
